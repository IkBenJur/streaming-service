version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "./internal/postgres/sqlc/video_queries.sql"
    schema: "./internal/postgres/migrations"
    gen:
      go:
        package: "repo"
        out: "./internal/postgres/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true